#include "main.h"

/**
 * _puts - prints a string
 * @str: string to be printed
 * Return: string
 */
void _puts(char *str)
{
	int 1;

	for (1 = 0; str[1] != '\0'; 1++)
	{
		_putchar(str[1];
	}
	_putchar('\n');
}
